export const apiKey = 'default';
export const password = 'password';
export const tokenLabel = 'development';

export const config = {
  authMode: 'basic',
  retryKey: 'id',
};

export function greet(name) {
  return `hello ${name}`;
}
